# 通用 Agent 设计 Skill 的边界

- `.cursor/skills/agent-design` 只提供 Agent runtime 的通用决策框架、技术设计模板和交付审查项；不包含任何项目、业务领域、供应商模型或实现框架的既定结论。
- 入口文件只保留适用范围、必要原则和 reference 路由，详细模板、选型标准与清单分别放入按需加载的 reference，避免把可复制 skill 变为某个仓库的架构说明。
- 使用 skill 时，AI 必须先检查目标项目的架构、权限、数据治理与运行时约束，并将发现到的具体事实写入本次设计文档，而不是写入该通用 skill。
