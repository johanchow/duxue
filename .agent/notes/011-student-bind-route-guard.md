# 学生绑定页路由守卫

- 现象：登录页选择“我是孩子”后点击“输入绑定码进入”无视觉响应。
- 根因：未登录路由守卫只放行 `/login`，`context.go('/ward-bind')` 随即又被重定向回登录页。
- 处理：将 `/ward-bind` 作为公开入口，并抽出 `appRedirect` 为可单测函数；未登录访问 Guardian 页面仍跳转登录，绑定后续 API 仍由绑定码与 PIN 鉴权。
