venv
output
__pycache__/
*.pyc
*.pyo
*.pyd
*.pem
.DS_Store

# 环境变量：通配所有 .env*，只放行模板文件。
# 仅忽略 .env 和 .env.prod 是不够的——.env.dev 同样含数据库凭证。
.env*
!.env.example

# 帧图片素材（含真实场景照片，属敏感数据，不入 Git）
duxue-server/data/frames/
duxue-server/data/uploads/
duxue-server/*.db

# 移动端构建产物与本机配置
duxue-cam/.gradle/
duxue-cam/local.properties
duxue-cam/**/build/
duxue-app/.dart_tool/
duxue-app/build/

www.duxuelai.xyz_nginx
duxue-cam/.idea/
.metadata
.cursor/mcp.json
