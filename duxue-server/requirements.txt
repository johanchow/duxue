# 服务端应用依赖（api / worker / beat 容器）
#
# 离线实验管线（试 prompt、标注、微调、评估）的依赖见 requirements-ml.txt。
# 两者刻意分开：torch + transformers 会让服务镜像膨胀到数 GB，
# 而服务端第一期不做任何本地模型推理，用不到它们。

# ── Web 框架
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
python-multipart>=0.0.12
python-dotenv>=1.0.0

# ── 数据库
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30.0
psycopg[binary]>=3.2.0
alembic>=1.14.0

# ── 任务队列
celery>=5.4.0
redis>=5.2.0

# ── 对象存储（帧图片，预签名 URL 直传）
oss2>=2.19.0

# ── AI 推理（百炼 Batch API，兼容 OpenAI 接口）
openai>=1.57.0
# 受控领域工作流；短期图状态使用 PostgreSQL checkpointer 持久化。
langgraph>=1.0.0
langgraph-checkpoint-postgres>=3.0.0

# ── 数值计算
numpy>=1.24.0

# ── 认证
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# ── 工具
pyyaml>=6.0
httpx>=0.28.0
websockets>=13.0

# ── 可观测性（OTEL 自动插桩，靠环境变量启用，零业务代码改动）
opentelemetry-distro>=0.50b0
opentelemetry-exporter-otlp>=1.29.0
opentelemetry-instrumentation-fastapi>=0.50b0
opentelemetry-instrumentation-sqlalchemy>=0.50b0
opentelemetry-instrumentation-celery>=0.50b0
opentelemetry-instrumentation-redis>=0.50b0
structlog>=24.4.0
